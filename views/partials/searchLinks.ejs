<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('./head.ejs') %>
    </head>

    <body>
        <div class="search__list" id="search__list">
            <% for (const item in urlLinks) { %>
            <a class="match" href="<%= urlLinks[item] %>" target="blank"><%= item.replaceAll("_", " ") %></a>
            <% } %>
        </div>
    </body>
</html>
