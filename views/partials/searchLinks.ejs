<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('./head.ejs') %>
    </head>

    <body>
        <div class="search__list" id="search__list">
            <% for (let i = 0; i < urlLinks.length; i++) { %>
            <a class="match" href="<%=urlLinks[i].link %>" data-category="<%=urlLinks[i].category %>" target="blank"><%= urlLinks[i].name.replaceAll("_", " ") %></a>
            <% } %>
        </div>
    </body>
</html>
