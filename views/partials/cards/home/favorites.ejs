<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">Figma</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">
            An all-in-one design platform. Design UX, UI, collaborate, build prototypes and more.
        </p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === 'lhabWVL') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">Google Fonts</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">Making the web more beautiful, fast, and open through great typography.</p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === 'aCbaa0Z') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">Unsplash</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">Beautiful, free images and photos that you can download and use for any project.</p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === 'Ja831i7') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">Font Awesome</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">Font Awesome is the world's most popular icon set and toolkit.</p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === '990s1G_') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">Udemy</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">Udemy is a leading destination for online courses with very affordable pricing.</p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === 'xTeNv_W') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">Dribbble</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">
            Inspirational designs, illustrations, and graphic elements from the world's best designers.
        </p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === 'dfA3mc2') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>
