<div class="card">
    <div class="card-top card-cyan">
        <h5 class="card__title">Grammar Checker</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">
            Nice little grammar checker to improve word usage, tense, and punctuation for any English text.
        </p>
        <div class="card__button-area">
            <button class="card__button button button-cyan">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === 'Iu7SXjP') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>
<div class="card">
    <div class="card-top card-cyan">
        <h5 class="card__title">Testimonials Generator</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">
            Generate testimonies for your website or product. No need to learn how to write a testimonial.
        </p>
        <div class="card__button-area">
            <button class="card__button button button-cyan">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === 'Zp9qtX6') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-top card-cyan">
        <h5 class="card__title">Lorem Ipsum Generator</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">Lorem Ipsum is simply dummy text of the printing and typesetting industry.</p>
        <div class="card__button-area">
            <button class="card__button button button-cyan">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === '7UdP-SJ') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>
