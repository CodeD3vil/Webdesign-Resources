<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">Live Color Picker</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">
            Live Color Picker is your best friend when looking for the right color on the internet.
        </p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === 'AJYT5Pv') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">Fonts Ninja</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">Turn on the extension on any website to discover which fonts and style were used.</p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === '44K1dWR') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">Nimbus Capture</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">Most likely the best screen capture extension i have ever used.</p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].id === '-TVvMCz') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="_blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>
