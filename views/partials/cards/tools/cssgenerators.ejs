<div class="card">
    <div class="card-top card-blue">
        <h5 class="card__title">Gradient Generator</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">Generate amazing gradients on the fly and use it directly as CSS code.</p>
        <div class="card__button-area">
            <button class="card__button button button-blue">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].name === 'CSS_Gradient_Generator')
                { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-top card-blue">
        <h5 class="card__title">Pattern Generator</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">Simple yet powerfull CSS pattern generator for your website background.</p>
        <div class="card__button-area">
            <button class="card__button button button-blue">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].name === 'CSS_Pattern_Generator')
                { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-top card-blue">
        <h5 class="card__title">Cubic Bezier Generator</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">Generate custom cubic-bezier curves for unique animations.</p>
        <div class="card__button-area">
            <button class="card__button button button-blue">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].name === 'Cubic_Bezier_Generator')
                { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>
