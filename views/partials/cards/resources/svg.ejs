<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">SVG Crop</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">Remove blank space from around any SVG images in an instant.</p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].name === 'SVG_Crop') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>
<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">Janvas SVG Editor</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">
            Janvas, is an online vector graphic editor for designers, illustrators and developers.
        </p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].name === 'Janvas_SVG_Editor') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-top card-yellow">
        <h5 class="card__title">SVG Viever and Optimizer</h5>
    </div>
    <div class="card-bottom">
        <p class="card__subtitle">View, edit and most importatly, optimize any SVG image directly in your browser.</p>
        <div class="card__button-area">
            <button class="card__button button button-yellow">
                <% for (let i = 0; i < urlLinks.length; i ++) { %> <% if (urlLinks[i].name ===
                'SVG_Viever_and_Optimize') { %>
                <a class="card__button-link" href="<%=urlLinks[i].link %>" target="blank">Go there</a>
                <% } %> <%} %>
            </button>
        </div>
    </div>
</div>
